# Admin System Architecture & Flow Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         SAMAJ APP                                │
│                   (Expo Router + React Native)                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      AUTHENTICATION                              │
│                                                                   │
│  ┌──────────────┐                         ┌──────────────┐      │
│  │ Login Screen │ ──────────────────────▶ │  AuthContext │      │
│  └──────────────┘                         └──────────────┘      │
│         │                                         │              │
│         │ POST /api/auth/login                    │              │
│         ▼                                         ▼              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Backend: JWT + User Object Returned               │   │
│  │         { token, user: { role: 'admin' | 'user' } }      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼                                   │
│                    ┌──────────────────┐                          │
│                    │  Role Checking   │                          │
│                    └──────────────────┘                          │
│                              │                                   │
│              ┌───────────────┴───────────────┐                  │
│              ▼                               ▼                  │
│    ┌──────────────────┐           ┌──────────────────┐         │
│    │  user.role ===   │           │  user.role ===   │         │
│    │     'admin'      │           │     'user'       │         │
│    └──────────────────┘           └──────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
              │                               │
              ▼                               ▼
┌──────────────────────┐           ┌──────────────────────┐
│   ADMIN ROUTE        │           │   USER ROUTE         │
│   /(admin)           │           │   /(tabs)            │
└──────────────────────┘           └──────────────────────┘
```

---

## Admin Route Structure

```
/(admin)
│
├── _layout.tsx ─────────────┐
│   • Tab Navigation          │  Navigation Guard:
│   • 4 Admin Tabs           │  ────────────────
│   • Navigation Guards       │  if (!isAdmin)
│                            │    → redirect to /login
│
├── index.tsx (Dashboard)
│   ┌──────────────────────────────────────────────────┐
│   │  • Total Users Card                              │
│   │  • Total Admins Card                             │
│   │  • Regular Users Card                            │
│   │  • Family Trees Card                             │
│   │  • Recent Signups List                           │
│   │  • Active Users List                             │
│   └──────────────────────────────────────────────────┘
│          │
│          ▼ GET /api/admin/dashboard
│   ┌──────────────────────────────────────────────────┐
│   │            Backend Response:                     │
│   │  {                                               │
│   │    totalUsers, totalAdmins, regularUsers,       │
│   │    totalFamilyTrees, recentSignups[],           │
│   │    activeUsers[]                                 │
│   │  }                                               │
│   └──────────────────────────────────────────────────┘
│
├── users.tsx (User Management)
│   ┌──────────────────────────────────────────────────┐
│   │  • Search Bar                                    │
│   │  • Role Filter Tabs (All / User / Admin)        │
│   │  • Paginated User List (10 per page)            │
│   │  • User Cards with Actions:                     │
│   │    ├─ View Details (👁️ Modal)                   │
│   │    ├─ Edit User (✏️ Modal)                       │
│   │    ├─ Change Role (🛡️ Confirmation)              │
│   │    └─ Delete User (🗑️ Confirmation)              │
│   └──────────────────────────────────────────────────┘
│          │
│          ├─ GET /api/admin/users?page=1&limit=10&search=...
│          ├─ GET /api/admin/users/:id
│          ├─ PUT /api/admin/users/:id (edit)
│          ├─ PUT /api/admin/users/:id/role (change role)
│          ├─ DELETE /api/admin/users/:id
│          └─ GET /api/admin/users/:id/family-tree (view only)
│
├── content.tsx (CMS)
│   ┌──────────────────────────────────────────────────┐
│   │  • Page List with Status Badges                  │
│   │    (PUBLISHED 🟢 / DRAFT 🟠)                     │
│   │  • Create Page Modal:                            │
│   │    ├─ Page Name (URL slug)                       │
│   │    ├─ Display Name                               │
│   │    ├─ Sections[] (dynamic)                       │
│   │    │  ├─ Title                                   │
│   │    │  ├─ Text (multiline)                        │
│   │    │  └─ Image URL                               │
│   │    └─ Published Toggle                           │
│   │  • Edit Page Modal (same fields)                 │
│   │  • Delete Page (confirmation)                    │
│   └──────────────────────────────────────────────────┘
│          │
│          ├─ GET /api/admin/pages
│          ├─ GET /api/admin/pages/:pageName
│          ├─ POST /api/admin/pages (create)
│          ├─ PUT /api/admin/pages/:pageName (update)
│          └─ DELETE /api/admin/pages/:pageName
│
└── media.tsx (Image Upload)
    ┌──────────────────────────────────────────────────┐
    │  • Upload Buttons:                               │
    │    ├─ Choose from Gallery 📷                     │
    │    └─ Take Photo 📸                              │
    │  • Upload Guidelines Card                        │
    │  • Recently Uploaded Images:                     │
    │    ├─ Thumbnail Preview                          │
    │    ├─ Full URL                                   │
    │    └─ Copy URL Button 📋                         │
    │  • How-to Instructions                           │
    └──────────────────────────────────────────────────┘
           │
           └─ POST /api/admin/upload-image
              (multipart/form-data)
              ▼
       Returns: { imageUrl: '/uploads/filename.jpg' }
```

---

## User Route Structure (Unchanged)

```
/(tabs)
│
├── _layout.tsx
│   • User Tab Navigation
│   • Existing tabs
│
├── index.tsx (Home)
├── explore.tsx
├── family-tree.tsx
│   └── User can ADD/EDIT own family tree ✅
└── ... (other existing screens)
```

---

## Authentication Flow Diagram

```
┌─────────────┐
│ User Opens  │
│    App      │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│  AuthContext    │
│ loadStoredAuth()│
└──────┬──────────┘
       │
       ├─ Token exists? ──No──▶ Show Login Screen
       │
       ▼ Yes
┌─────────────────────────────────┐
│ GET /api/auth/me (with token)   │
│ Fetch user profile               │
└──────┬──────────────────────────┘
       │
       ├─ Success ──▶ User object returned
       │              ▼
       │         ┌───────────────┐
       │         │ Check role    │
       │         └───────┬───────┘
       │                 │
       │    ┌────────────┴────────────┐
       │    ▼                         ▼
       │ role='admin'             role='user'
       │    │                         │
       │    ▼                         ▼
       │ /(admin)                 /(tabs)
       │
       ├─ Error (401) ──▶ Clear token, show Login
       │
       └─ Error (500) ──▶ Retry or show error


┌──────────────────────────────────────────────────────────┐
│                   LOGIN PROCESS                          │
└──────────────────────────────────────────────────────────┘

User enters credentials
       │
       ▼
┌───────────────────────────────────────────────┐
│ POST /api/auth/login                          │
│ Body: { email, password }                     │
└───────────────────┬───────────────────────────┘
                    │
       ┌────────────┴────────────┐
       ▼                         ▼
    Success                   Failure
       │                         │
       ▼                         ▼
┌────────────────┐      ┌──────────────────┐
│ Response:      │      │ Show error       │
│ {              │      │ "Invalid creds"  │
│   token: JWT,  │      └──────────────────┘
│   user: {...}  │
│ }              │
└───────┬────────┘
        │
        ▼
┌─────────────────────────────────┐
│ 1. Store token in SecureStore   │
│ 2. Set user in AuthContext      │
│ 3. Check user.role               │
└───────┬─────────────────────────┘
        │
        ├─ role='admin' ──▶ router.replace('/(admin)')
        │
        └─ role='user' ──▶ router.replace('/(tabs)')
```

---

## API Request Flow

```
┌──────────────────┐
│  Admin Screen    │
│  (e.g., Users)   │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  adminAPI.getUsers()                    │
│  (from services/api.ts)                 │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Axios Request:                         │
│  GET /api/admin/users                   │
│  Headers: {                             │
│    Authorization: 'Bearer <token>'      │
│  }                                      │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│         Backend Middleware              │
│  1. Extract token from Authorization    │
│  2. Verify JWT signature                │
│  3. Decode user from token              │
│  4. Check user.role === 'admin'         │
└────────┬────────────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
Authorized  Unauthorized
    │         │
    ▼         ▼
 Execute   Return 401
 Handler   "Unauthorized"
    │
    ▼
┌─────────────────────────────────────────┐
│  Database Query                         │
│  User.find().limit(10)                  │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Response:                              │
│  {                                      │
│    success: true,                       │
│    data: [...users],                    │
│    pagination: {...}                    │
│  }                                      │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Frontend receives response             │
│  Update UI with user list               │
└─────────────────────────────────────────┘
```

---

## Data Models

### User Model
```
┌──────────────────────────────────────────┐
│              User                        │
├──────────────────────────────────────────┤
│ _id: ObjectId                            │
│ name: String (required)                  │
│ email: String (required, unique)         │
│ password: String (hashed, required)      │
│ memberId: String (optional)              │
│ phone: String (optional)                 │
│ role: String ('user' | 'admin')          │
│ createdAt: Date                          │
│ updatedAt: Date                          │
└──────────────────────────────────────────┘
```

### PageContent Model (CMS)
```
┌──────────────────────────────────────────┐
│           PageContent                    │
├──────────────────────────────────────────┤
│ _id: ObjectId                            │
│ pageName: String (unique, URL slug)      │
│ displayName: String (display title)      │
│ sections: Array<Section>                 │
│   ├─ title: String                       │
│   ├─ text: String                        │
│   ├─ imageUrl: String                    │
│   └─ order: Number                       │
│ isPublished: Boolean                     │
│ lastModifiedBy: ObjectId (User ref)      │
│ createdAt: Date                          │
│ updatedAt: Date                          │
└──────────────────────────────────────────┘
```

### FamilyTree Model (Existing, User-Only)
```
┌──────────────────────────────────────────┐
│          FamilyTree                      │
├──────────────────────────────────────────┤
│ _id: ObjectId                            │
│ user: ObjectId (User ref)                │
│ members: Array<Member>                   │
│   ├─ name: String                        │
│   ├─ relation: String                    │
│   ├─ ... (other fields)                  │
│ createdAt: Date                          │
│ updatedAt: Date                          │
└──────────────────────────────────────────┘

⚠️ Admin Access: READ-ONLY (count only)
✅ User Access: FULL CRUD
```

---

## Component Hierarchy

```
App Root (_layout.tsx)
│
├─ AuthProvider (AuthContext)
│  │
│  ├─ Stack Navigator
│  │  │
│  │  ├─ (tabs) ──────────────┐
│  │  │  └─ User Screens       │ Regular Users
│  │  │                        │
│  │  ├─ (admin) ──────────────┼───┐
│  │  │  ├─ Dashboard          │   │
│  │  │  ├─ Users              │   │ Admins Only
│  │  │  ├─ Content            │   │
│  │  │  └─ Media              │   │
│  │  │                        │   │
│  │  ├─ login ────────────────┼───┼── Everyone
│  │  ├─ signup ───────────────┼───┼── Everyone
│  │  └─ ... (other screens)   │   │
│  │                            │   │
│  └─ Navigation Guards ────────┼───┘
│     • Check isAdmin           │
│     • Redirect if unauthorized│
│                                │
└─ SecureStore (Token Storage)  │
   • JWT token persisted        │
   • Auto-load on app start     │
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────┐
│                   Security Stack                         │
└─────────────────────────────────────────────────────────┘

Layer 1: Frontend Route Protection
┌────────────────────────────────────────┐
│ (admin)/_layout.tsx                    │
│ • Check isAdmin on mount               │
│ • Redirect non-admins to /login        │
└────────────────────────────────────────┘
         │
         ▼ User tries to access
         │
Layer 2: API Request Headers
┌────────────────────────────────────────┐
│ Authorization: Bearer <token>          │
│ • JWT token included in every request  │
│ • Token stored securely in SecureStore │
└────────────────────────────────────────┘
         │
         ▼ Request sent
         │
Layer 3: Backend Middleware
┌────────────────────────────────────────┐
│ protect() middleware                   │
│ • Verify JWT signature                 │
│ • Decode user from token               │
│ • Attach user to req.user              │
└────────────────────────────────────────┘
         │
         ▼ Token valid
         │
Layer 4: Role Authorization
┌────────────────────────────────────────┐
│ authorize('admin') middleware          │
│ • Check req.user.role === 'admin'      │
│ • Return 403 if not admin              │
└────────────────────────────────────────┘
         │
         ▼ Role authorized
         │
Layer 5: Database Access Control
┌────────────────────────────────────────┐
│ Route Handlers                         │
│ • Validate input                       │
│ • Check permissions                    │
│ • Execute query                        │
│ • Return sanitized response            │
└────────────────────────────────────────┘
         │
         ▼ Success
         │
    Response to client
```

---

## Error Handling Flow

```
Error Occurs
    │
    ├─ Network Error ──────────────────▶ Show offline message
    │                                    + Retry button
    │
    ├─ 401 Unauthorized ───────────────▶ Clear token
    │                                    + Redirect to /login
    │                                    + Show "Session expired"
    │
    ├─ 403 Forbidden ──────────────────▶ Show "Access denied"
    │                                    + Redirect to appropriate route
    │
    ├─ 404 Not Found ──────────────────▶ Show "Resource not found"
    │                                    + Back button
    │
    ├─ 500 Server Error ───────────────▶ Show "Server error"
    │                                    + Retry button
    │                                    + Log to monitoring service
    │
    └─ Validation Error ───────────────▶ Show field-specific errors
                                        + Highlight invalid fields
```

---

## File Upload Flow

```
User selects image
    │
    ▼
┌─────────────────────────────────────┐
│ expo-image-picker                   │
│ • Request permissions (first time)  │
│ • Open gallery or camera            │
│ • Return image URI                  │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Create FormData                     │
│ • Extract filename from URI         │
│ • Determine MIME type               │
│ • Append to 'image' field           │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ POST /api/admin/upload-image        │
│ • Content-Type: multipart/form-data │
│ • Authorization: Bearer <token>     │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Backend: multer middleware          │
│ • Validate file size (<5MB)         │
│ • Validate file type (images only)  │
│ • Generate unique filename          │
│ • Save to /uploads directory        │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Response:                           │
│ {                                   │
│   success: true,                    │
│   imageUrl: '/uploads/12345.jpg'    │
│ }                                   │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Frontend displays:                  │
│ • Image thumbnail                   │
│ • Full URL                          │
│ • Copy button                       │
└─────────────────────────────────────┘
```

---

## State Management

```
┌─────────────────────────────────────────────────────────┐
│                    AuthContext                          │
│  (Global Authentication State)                          │
├─────────────────────────────────────────────────────────┤
│ State:                                                  │
│  • user: User | null                                    │
│  • loading: boolean                                     │
│  • isAdmin: computed (user?.role === 'admin')           │
│                                                         │
│ Methods:                                                │
│  • login(email, password): Promise<User>                │
│  • signup(userData): Promise<User>                      │
│  • logout(): void                                       │
│  • refreshUser(): Promise<void>                         │
│  • loadStoredAuth(): Promise<void>                      │
└─────────────────────────────────────────────────────────┘
                      │
         ┌────────────┴────────────┐
         ▼                         ▼
┌──────────────────┐    ┌──────────────────┐
│  Admin Screens   │    │  User Screens    │
│  • Dashboard     │    │  • Home          │
│  • Users         │    │  • Explore       │
│  • Content       │    │  • Family Tree   │
│  • Media         │    │  • ...           │
└──────────────────┘    └──────────────────┘
    │                       │
    ├─ Local State:         ├─ Local State:
    │  • users[]            │  • familyTree[]
    │  • pages[]            │  • events[]
    │  • stats{}            │  • ...
    │  • uploadedImages[]   │
    │  • ...                │
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Production Setup                      │
└─────────────────────────────────────────────────────────┘

Frontend (Expo App)
┌────────────────────────────────┐
│   App Stores                   │
│  ┌──────────┐  ┌──────────┐   │
│  │   iOS    │  │ Android  │   │
│  │  Store   │  │   Play   │   │
│  └────┬─────┘  └─────┬────┘   │
│       │              │         │
│       └──────┬───────┘         │
└──────────────┼─────────────────┘
               │
               ▼
        User Devices
               │
               │ HTTPS
               ▼
┌────────────────────────────────┐
│      Backend Server            │
│  (Render/Heroku/AWS)           │
│                                │
│  ┌──────────────────────────┐ │
│  │   Node.js + Express      │ │
│  │   • API Routes           │ │
│  │   • JWT Auth             │ │
│  │   • File Upload          │ │
│  │   • Admin Middleware     │ │
│  └────────────┬─────────────┘ │
└───────────────┼───────────────┘
                │
                ▼
┌────────────────────────────────┐
│      MongoDB Atlas             │
│  • Users Collection            │
│  • PageContent Collection      │
│  • FamilyTree Collection       │
│  • Automatic Backups           │
│  • Encrypted at Rest           │
└────────────────────────────────┘
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────────┐
│              Performance Strategies                      │
└─────────────────────────────────────────────────────────┘

1. API Response Optimization
   ┌──────────────────────────────┐
   │ • Pagination (10 items/page) │
   │ • Selective field projection │
   │ • Database indexing          │
   │ • Response caching           │
   └──────────────────────────────┘

2. Frontend Optimization
   ┌──────────────────────────────┐
   │ • Lazy loading screens       │
   │ • Image compression          │
   │ • Minimal re-renders         │
   │ • FlatList for long lists    │
   └──────────────────────────────┘

3. Network Optimization
   ┌──────────────────────────────┐
   │ • Request debouncing         │
   │ • Retry logic with backoff   │
   │ • Connection pooling         │
   │ • CDN for static assets      │
   └──────────────────────────────┘

4. State Management
   ┌──────────────────────────────┐
   │ • Context API (lightweight)  │
   │ • Local state only           │
   │ • Minimal global state       │
   │ • Computed properties        │
   └──────────────────────────────┘
```

---

## Monitoring & Logging

```
┌─────────────────────────────────────────────────────────┐
│                 Monitoring Stack                         │
└─────────────────────────────────────────────────────────┘

Application Logs
┌────────────────────────────────┐
│ • API request logs             │
│ • Error logs with stack traces │
│ • User action logs             │
│ • Performance metrics          │
└────────────┬───────────────────┘
             │
             ▼
     Monitoring Service
┌────────────────────────────────┐
│ • Sentry (Error tracking)      │
│ • LogRocket (Session replay)   │
│ • New Relic (Performance)      │
│ • Datadog (Infrastructure)     │
└────────────┬───────────────────┘
             │
             ▼
      Alert System
┌────────────────────────────────┐
│ • Email notifications          │
│ • Slack/Discord webhooks       │
│ • PagerDuty (Critical)         │
└────────────────────────────────┘
```

---

**Document Version**: 1.0  
**Last Updated**: ${new Date().toISOString().split('T')[0]}  
**Architecture Status**: COMPLETE
